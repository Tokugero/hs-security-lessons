---
apiVersion: v1
kind: Service
metadata:
  name: registry
  namespace: shared-services
  annotations:
    metallb.universe.tf/loadBalancerIPs: "{{ registry_ip }}"
spec:
  ports:
  - name: web
    port: 80
    targetPort: 5000
  selector:
    app: registry
  type: LoadBalancer